# CLAUDE_REVIEW_PROMPT — WI-07-0.2.3 (Excel persistence adapter)

Generated by DevBridge DB-M07 at 2026-08-30. This is the **exact prompt** to copy to Claude
for the focused review of WI-07-0.2.3. Copy everything between the `---BEGIN---` and
`---END---` fences verbatim. Do not add instructions before or after it. The prompt is
self-contained; it references `REVIEW_PACKET.md` and `CLAUDE_REVIEW_FILES.md`, which must be
provided alongside it.

DevBridge does NOT call Claude automatically in this milestone — the copy is a human action.

---

```text
---BEGIN---
Focused code review — WI-07-0.2.3 (Excel persistence adapter), Change CHG-20260830-016.

You are reviewing an implementation that has ALREADY been deterministically verified by
DevBridge DB-M06 (result: VERIFICATION_PASSED, 22/22 parts). Your job is the focused human
review: read the review package, confirm the implementation against the acceptance criteria
and review scope, and return one decision in the exact format at the end.

READ FIRST — in this order:
1. REVIEW_PACKET.md  (the complete, evidence-rich review package; authoritative)
2. CLAUDE_REVIEW_FILES.md  (the exact file set you may open)

The package embeds the complete content / review representation of the three untracked
implementation files, so you generally do not need to open them. If you want line-level
confirmation, open ONLY the files listed in CLAUDE_REVIEW_FILES.md under
C:\Personal\Nexus.Developer\src\Nexus.Developer.Infrastructure\DevelopmentControl\.
Do not open anything else in the repository, and do not re-read the authoritative workbook.

WHAT YOU ARE REVIEWING
- Node: WI-07-0.2.3 "Excel persistence adapter" (WorkItem, P0, GATE_A, parent M-07-0.2)
- Goal (authoritative workbook field): "ExcelDevelopmentControlStore implements read + one
  safe mutation end-to-end, atomically."
- Change: CHG-20260830-016 (Active Changes row 79). Baseline: feature/m-08-1-2-ci-pipeline
  @ ea39db910a6e3b00bff880316996a696ae7460dc, 0 commits made by implementation.
- Delta: exactly 3 new files under src/Nexus.Developer.Infrastructure/DevelopmentControl/**
  (ExcelWorkbookColumnMap.cs, DevelopmentControlCellCodec.cs, ExcelDevelopmentControlStore.cs).
  No pre-existing file was modified; the contract IDevelopmentControlStore was not modified.

ACCEPTANCE CRITERIA (8, from the governed handoff / acceptance matrix — all already PASS):
1. ExcelDevelopmentControlStore — the ClosedXML-backed IDevelopmentControlStore adapter —
   under the reserved glob.
2. Full 22-op contract; every mutating op takes MutationEnvelope and returns MutationResult<T>.
3. Read + one safe mutation end-to-end, atomically. Concurrency/locking/multi-op atomic
   writes are WI-07-0.2.4 — not built here.
4. Canonical NEXUS_DEVELOPMENT_CONTROL.xlsx is the authoritative store; Session Protocol
   append-only; history never rewritten.
5. Activity Log appended with the 34-column schema.
6. Reuse existing assets (contracts, validator/report/migration, ClosedXML); no rebuild.
7. Strictly inside the reserved scope.
8. Build/test completion standard (0 warnings/0 errors; 199/199 tests; 32/32 harness checks).

VERIFICATION EVIDENCE (from state\verification.json, not re-derived by you)
- Canonical workbook SHA256 F52A1A8FE491B6C91809EAC92DB2F0DCCD43FF7C5C80BA2618E19B64D3FDD4F7
  unchanged before/after all harness runs (not CANONICAL_WORKBOOK_MODIFIED).
- Append-only: all 956 canonical Version History rows byte-identical in the mutated copy, 9
  appended (958-966); all 52 canonical Activity Log rows byte-identical, 12 appended (54-65).
- Build: 4 src projects Release, 0 warnings / 0 errors. Tests: 199 passed / 0 failed / 0
  skipped (independent re-run). Harness: 32/32 checks (every mutating op exercised).
- Independent control-state count cross-check matches store output exactly (636 live / 166
  milestones / 138 work items / 0 blocked / 19 active changes / 18 open audit findings).

KNOWN LIMITATIONS (DB-M06 classification — NOT defects, do not turn them into FIX REQUIRED)
- EXPECTED_NEXT_WORK (WI-07-0.2.4): multi-node/subtree atomic writes (ReparentNodeAsync
  carries only the single node); concurrency/locking/multi-op atomic writes.
- ACCEPTABLE_CURRENT_LIMITATION: IdempotencyKey not persisted as its own field (idempotency
  via Activity Log scan of operation + change id + entity id, in the same atomic save);
  RunPreflightAsync first-pass heuristics (naive path-prefix glob overlap, no wildcards).
- PRE_EXISTING_DATA_CONDITION: live workbook does not maintain Is Current=Yes consistently
  (IsDeleted is derived from terminal status; validation is structural only);
  ControlState.RoadmapVersion is empty (no "roadmap vX.Y" marker in the narrative);
  dependencies may reference non-roadmap decision entities (DEC-002/DEC-003).

BINDING REVIEW RULES
1. Do NOT scan the whole repository by default. Open only the files in CLAUDE_REVIEW_FILES.md
   if needed.
2. Do NOT re-read the authoritative NEXUS_DEVELOPMENT_CONTROL.xlsx to re-derive governance,
   and do NOT modify it in any way.
3. Do NOT select a different or next Nexus task, and do NOT redesign the roadmap.
4. Do NOT broaden the reserved scope or demand scope not in the work item.
5. Do NOT update Development Control completion state (status, Version History, Active
   Changes, Activity Log, roadmap).
6. Do NOT commit, stage, push, or create branches.
7. Do NOT implement fixes, refactors, or new features during this review — you review and
   decide only. Any required fix is a separate governed step.

You may run read-only verification (open named files; non-mutating build/test commands) to
confirm a claim, but you must not alter the workbook or source.

OUTPUT — produce exactly the following format, nothing else:

REVIEW RESULT

Node: WI-07-0.2.3
Change ID: CHG-20260830-016

Decision: PASS | FIX REQUIRED

Severity: NONE | MINOR | MAJOR | BLOCKING          (FIX REQUIRED only)
Review scope: IN_SCOPE | SCOPE_VIOLATION           (FIX REQUIRED only)
Fix location: <file path(s)>                       (FIX REQUIRED only)
Fix summary: <one to three sentences>              (FIX REQUIRED only)
Reviewed files:
  - <path> (PASS | ISSUE — one line)
Verification notes:
  <brief, evidence-grounded notes on what was checked>

A PASS means the implementation meets the 8 acceptance criteria and the review scope
boundaries, with only the documented limitations above. A FIX REQUIRED must state severity,
whether the issue is inside the reserved scope, and where/what the fix is — it is not a
mandate to implement the fix in this step.
---END---
```
