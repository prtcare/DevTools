<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>NexusDevInteropProbe</AssemblyName>
    <RootNamespace>NexusDevInteropProbe</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Lane B (SP1-M02 live interop proof): the probe binds the REAL Nexus.Developer
         composition root (AddDevelopmentControl) exactly as Lane A wired it, so the
         Developer side of the cross-process proof is the genuine guarded store + the
         genuine NamedDevelopmentControlWriteLockFactory (Mutex). Nothing in any repo is
         modified; this project lives entirely under %TEMP%. -->
    <ProjectReference Include="C:\Personal\Nexus-W3-M05\src\Nexus.Developer.Infrastructure\Nexus.Developer.Infrastructure.csproj" />
    <PackageReference Include="ClosedXML" Version="0.105.1" />
    <PackageReference Include="Microsoft.Extensions.DependencyInjection" Version="10.0.11" />
  </ItemGroup>

</Project>
