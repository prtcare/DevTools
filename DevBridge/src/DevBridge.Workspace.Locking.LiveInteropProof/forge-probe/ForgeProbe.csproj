<Project Sdk="Microsoft.NET.Sdk">

  <PropertyGroup>
    <OutputType>Exe</OutputType>
    <TargetFramework>net10.0</TargetFramework>
    <ImplicitUsings>enable</ImplicitUsings>
    <Nullable>enable</Nullable>
    <AssemblyName>ForgeInteropProbe</AssemblyName>
    <RootNamespace>ForgeInteropProbe</RootNamespace>
  </PropertyGroup>

  <ItemGroup>
    <!-- Lane B (SP1-M02 live interop proof): the Forge side uses the real M02 library
         (DevBridge.Workspace.Locking) exactly as Forge/DevTools writers would. Nothing in
         any repo is modified; this project lives entirely under %TEMP%. -->
    <ProjectReference Include="C:\Personal\DevTools-W2-M02\DevBridge\src\DevBridge.Workspace.Locking\DevBridge.Workspace.Locking.csproj" />
  </ItemGroup>

</Project>
